{{yield }} 
    <Appointments @arrayDays={{this.arrayDays}} @finder={{this.finder}}  @updateArray={{this.updateArray}}></Appointments> 
    <Selecteddays @arrayDays={{this.arrayDays}} @finder={{this.finder}} @updateCleared={{this.updateCleared}}></Selecteddays>

